#include "main.h"

/**
 * main - tests the positive_or_negative function with 0 as input
 *
 * Return: Always 0
 */

int main(void)
{
    int zero = 0;

    positive_or_negative(zero);

    return (0);
}
